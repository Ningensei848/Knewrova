#!/usr/bin/env bash
# .script/__DoNotTouch/hooks/pre-push

set -eu
hook_dir="$(dirname "$0")"

# ps1版を廃止し、整理されたshスクリプトを直列で実行
bash "$hook_dir/check-push-policy.sh" "$@" || exit $?
bash "$hook_dir/collision-check-and-lock.sh" "$@" || exit $?

exit 0
